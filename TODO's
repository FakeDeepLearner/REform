1) Add a notification for admins to notify them that they still have reports to look at when they log in.
2) Fix a bug that prevents reports from changing their state to RESOLVED, if the admin chooses not to remove the report.