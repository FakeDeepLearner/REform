import java.util.HashMap;

public class Manager {


    // HashMap to store Users and their IDs
    private static HashMap<User, Integer> userIDMap = new HashMap<User, Integer>();

    public static boolean checkUserExists(User u){
    /*
    Return true if User <u> exist in the userIDMap.
     */

    }

    public static boolean callUserLogin(User u){
        /*
        Return True if User <u> successfully logged in
         */
    }

    public static String[] callUserLoginHistory(User u){
        /*
        Calls the loginHistory method from User <u> and return an array of String
        that contains the login history of the User
         */
    }

}
